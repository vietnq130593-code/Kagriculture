# V62 MetaBalance | Kaggriculture

A reproducible public-state route portfolio for Kaggriculture. This notebook
rebuilds the exact single-file agent used by the strongest historical
submission in this account (submission 55958242, public score 2763.2).

The output is a deterministic `submission.tar.gz` containing `main.py`.
The source retains its upstream Apache-2.0 notices and attribution.


---

## Rebuild and verify the scored artifact


---

## Reproducibility note

The 2763.2 value is the historical public score of submission 55958242; it is
evidence of the artifact's prior result, not a promise that a new run or
future opponent pool will reproduce it. Running this notebook only creates
the archive and does not spend a competition submission.
